    <form action = "" method = "post">
        <input type="submit" name="upvote" value="Upvote" />
    </form>

    delete urlDatabase.req.params.id;

    curl -X POST "http://localhost:8080/urls/diablo/delete"

    curl -X "http://localhost:8080/urls/diablo/delete"


res.status(400);
res.send('None shall pass')    

commit and push all changes to the master branch
git branch feature/cookies
git show-branch
git checkout feature/cookies

To Merge:
Commit to feature branch (git push --set-upstream origin feature/user-registration)
git checkout master
git merge origin/feature/user-registration

git branch feature/specific-features
git show-branch
git checkout feature/specific-features