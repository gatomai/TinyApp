<!DOCTYPE html>
<html lang="en">

<head>
</head>

<body>
    <!-- Add your EJS code here -->
    <% include ./partials/_header %>

        <p>
            <%for(var element in urls){ %>
                <% if (element === shortURL ) { %>
                    <%= element %> :
                        <%= urls[element] %>
                            <br>
                            <% } %>
                                <% } %>
        </p>


</body>

</html>